# Setup

## Prerequisities

Node.js

## Install dependencies

`npm install`

## Run dev server (with hot-reload)

`npm run dev`

## Linting

Before committing website changes to the master branch, run the linter to avoid pipeline failures:
`npx prettier . --write`
